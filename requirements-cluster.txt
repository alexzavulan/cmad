# Cluster-only dependencies, installed with `pip install -r requirements-cluster.txt`
# inside a venv built with `python -m venv --system-site-packages`, constrained by
# constraints-cluster.txt (generated from the container's own `pip freeze`) so that
# torch/torchvision/triton are never touched here -- they come from the container.

anomalib
hydra-core>=1.3.6
matplotlib>=3.11.2
pandas>=3.0.5
pyarrow>=25.0.1
scikit-learn>=1.9.1
pytest>=9.1.1
